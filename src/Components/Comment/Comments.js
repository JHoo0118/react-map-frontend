import React from "react";
import styled from "styled-components";

const Container = styled.div`
  width: 100%;
  background-color: yellow;
`;

export const Comments = ({ classes }) => {
  <Container>댓글</Container>;
};
