import React from "react";
import styled from "styled-components";
import { device } from "./Device";

const Container = styled.div`
  flex-grow: 1;
`;

const Picture = styled.div`
  height: 50px;
  border-radius: 90%;
  margin-right
`;

export default () => <Container>헤더</Container>;
